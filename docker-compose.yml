pywb:
    build: ./pywb
    restart: always
    links:
        - redis

redis:
    image: redis:latest
    restart: always
# remote redis
#    command: redis-server --slaveof test 6379

#shared_data:
#    image: python:2.7
#    command: python -i
#    volumes:
#        - ./shareddata:/shareddata

app:
    build: ./app
    restart: always
    links:
        - redis

    volumes:
        - /var/run/docker.sock:/var/run/docker.sock
        - ./app/browser:/browser

    environment:
        - SCREEN_WIDTH=1200
        - SCREEN_HEIGHT=800

    ports:
        - 9020:9020
